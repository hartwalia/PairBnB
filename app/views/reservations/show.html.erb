</br></br>

<h1 class='text-center'><%= "You have booked #{@reservation.listing.name}" %></h1>

</br></br>

<div class="container">

</br></br>

	<div class="row">
		<div class="col-md-8 col-md-offset-2">
			<div class="thumbnail">
				<div class="caption-full">
					</br>
					<div class="pull-right"><b>By | </b><%= @reservation.listing.user.name %></div>
					<h4 class="text-left"><strong>About this listing</strong></h4>
					<p><%= @reservation.listing.description %></p>
					</br>

					<h4 class="text-left"><strong>Reservation Details</strong></h4>
					</br>

					<div class="row">
						<div class="col-md-5 col-md-offset-1">
							<ul class="list-group">
								<li class="list-group-item">
									<span class="badge"><%= @reservation.check_in %></span>
									Check In
								</li>
								</br>
								<li class="list-group-item">
									<span class="badge"><%= @reservation.duration %></span>
									Duration
								</li>
							</ul>
						</div>

						<div class="col-md-5">
							<ul class="list-group">
								<li class="list-group-item">
									<span class="badge">$ <%= @reservation.total_cost %></span>
									Total
								</li>
								</br>
								<li class="list-group-item">
									<span class="badge"><%= @reservation.paid %></span>
									Payment
								</li>
							</ul>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>

<% if signed_in? %>
		<div class="container">
			<div class="row">
				<div class="col-md-6 col-md-offset-2">
					<%= link_to 'Cancel', edit_listing_path(@reservation.listing.id), class: "btn btn-warning btn-sm" %>
					<%= link_to 'Confirm', new_reservation_transaction_path(@reservation), class: "btn btn-warning btn-sm" %>
					<%= link_to 'See More', listing_path(@reservation.listing.id), class: "btn btn-warning btn-sm" %>
				</div>
			</div>
		</div>
<% end %>